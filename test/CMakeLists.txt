# ── Test CMakeLists.txt for NMFD ──────────────────────────────────────────────
# Each test directory gets its own CMakeLists.txt

add_subdirectory(detail)
add_subdirectory(operations)
add_subdirectory(solvers)
add_subdirectory(cahn_hilliard)
